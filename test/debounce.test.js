'use strict';

const expect = require('chai').expect;
const sinon = require('sinon');
let debounce = require('../questions/debounce');

describe('Debounce', () => {
    it('should work with a single input', () => {
        //Remove when writing test
        expect(false).to.equal(true);
    });

    it('should only execute once on multiple calls', () => {
        //Remove when writing test
        expect(false).to.equal(true);
    });

    it('should execute immediately if immediate == true', () => {
        //Remove when writing test
        expect(false).to.equal(true);
    });
});
